eda_credentials:
  - name: "Red Hat Ansible Automation Platform Credential"
    description: "Red Hat Ansible Automation Platform Credential"
    org: "Default"
    type: "Red Hat Ansible Automation Platform"
    inputs:
      host: "{{ aap_host | trim('/') }}/api/controller/"
      username: "{{ aap_username }}"
      password: "{{ aap_password }}"

  - name: "Quay Registry Credential"
    description: "Quay Registry Credential"
    org: "Default"
    type: "Container Registry"
    inputs:
      host: "quay.io"
      username: "{{ quay_username }}"
      password: "{{ quay_password }}"

  - name: "ITSM App Credential"
    description: "ITSM App API credentials (itsm-app)"
    org: "Default"
    type: "ITSM App"
    inputs:
      itsm_api_base_url: "{{ itsm.api_base_url }}"
      itsm_api_user: "{{ itsm.eda_user.username }}"
      itsm_api_password: "{{ itsm.eda_user.password }}"